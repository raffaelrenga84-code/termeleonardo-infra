# Ordini dal QR dopo il pagamento — piano di lavoro

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** vedere, rimborsare, annullare e ristampare gli ordini dal QR dal back office e dal palmare; avvisare quando la stampante tace; riordinare le schede del back office in quattro famiglie.

**Architecture:** regole pure in `rimborsi.ts` (provate da sole); azioni nuove nella funzione `pos` (solo cloud: Stripe e l'archivio degli ordini vivono lì, il PC del Bistrot risponde 503 come per le altre azioni cloud); la pagina del palmare e il back office leggono e mostrano. Spec: `docs/superpowers/specs/2026-09-05-ordini-qr-design.md`.

**Tech Stack:** Deno (Supabase Edge Function `pos`), Postgres via supabase-js, pagine HTML/JS pure, prove Deno sul sorgente (`deno test --node-modules-dir=none . --allow-read --allow-env --allow-write`), deploy `npx supabase functions deploy pos --project-ref mvuiuwakuseockotlcnp --no-verify-jwt` col token in `C:/Users/admin/token-supabase.txt`.

## Global Constraints

- Mai toccare le stampanti fiscali (192.168.0.51/.52, 8989/8990).
- Le modifiche a `index.ts` e alle pagine si fanno con script node ad ancore uniche (`unica()`), non con sed quando il testo ha backtick o `$`.
- Ogni azione nuova del palmare va in `azioniPalmare` e chiamata dalla pagina con `chiama(..., { cloud: true })`.
- Nomi in italiano, commenti che dicono il perché e la data della decisione (5 settembre 2026).
- Rimborsi Stripe: `POST /v1/refunds` con `payment_intent` e `amount` in centesimi; mai oltre il residuo.

---

### Task 1: il modulo puro dei rimborsi

**Files:**
- Create: `supabase/functions/pos/rimborsi.ts`
- Test: `supabase/functions/pos/rimborsi.test.ts`

**Interfaces:**
- Produces: `type OrdineRimborsabile = { totale_cent: number; rimborsato_cent?: number | null; modo: string; stato: string; stripe_pagamento?: string | null }`; `residuoRimborso(o): number`; `importoRimborso(o, richiesto: unknown): { ok: true; cent: number } | { ok: false; errore: string }`; `statoDopoRimborso(totale_cent, rimborsato_cent): 'rimborsato' | null`; `importoRiga(r: { quantita: number; prezzo_cent: number }): number`; `corpoRimborsoStripe(paymentIntent: string, cent: number): Record<string, string>`.

- [ ] **Step 1: la prova**

```ts
import { assertEquals } from 'jsr:@std/assert';
import { corpoRimborsoStripe, importoRiga, importoRimborso, residuoRimborso, statoDopoRimborso } from './rimborsi.ts';

const carta = { totale_cent: 2450, rimborsato_cent: 0, modo: 'carta', stato: 'in_cucina', stripe_pagamento: 'pi_1' };

Deno.test('quanto si puo ancora restituire: il totale meno quel che e gia tornato', () => {
  assertEquals(residuoRimborso(carta), 2450);
  assertEquals(residuoRimborso({ ...carta, rimborsato_cent: 1000 }), 1450);
  assertEquals(residuoRimborso({ ...carta, rimborsato_cent: 9999 }), 0, 'mai sotto zero');
});

Deno.test('l importo chiesto: vuoto = tutto il residuo; intero fra 1 e il residuo; con la carta serve il pagamento Stripe', () => {
  assertEquals(importoRimborso(carta, undefined), { ok: true, cent: 2450 });
  assertEquals(importoRimborso(carta, ''), { ok: true, cent: 2450 });
  assertEquals(importoRimborso(carta, 1000), { ok: true, cent: 1000 });
  assertEquals(importoRimborso(carta, '1000'), { ok: true, cent: 1000 });
  assertEquals(importoRimborso(carta, 2451).ok, false, 'oltre il residuo no');
  assertEquals(importoRimborso(carta, 0).ok, false);
  assertEquals(importoRimborso(carta, 12.5).ok, false, 'centesimi interi');
  assertEquals(importoRimborso({ ...carta, rimborsato_cent: 2450 }, undefined).ok, false, 'niente da restituire');
  assertEquals(importoRimborso({ ...carta, stripe_pagamento: null }, 100).ok, false, 'senza pagamento Stripe non si sa cosa restituire');
  assertEquals(importoRimborso({ ...carta, stato: 'in_attesa' }, 100).ok, false, 'un ordine mai pagato non si rimborsa');
  assertEquals(importoRimborso({ ...carta, modo: 'camera', stripe_pagamento: null }, 100), { ok: true, cent: 100 }, 'in camera si toglie dall addebito');
});

Deno.test('dopo il rimborso: tutto tornato = rimborsato, altrimenti lo stato resta', () => {
  assertEquals(statoDopoRimborso(2450, 2450), 'rimborsato');
  assertEquals(statoDopoRimborso(2450, 1000), null);
});

Deno.test('l importo di una riga e il corpo per Stripe', () => {
  assertEquals(importoRiga({ quantita: 3, prezzo_cent: 950 }), 2850);
  assertEquals(corpoRimborsoStripe('pi_1', 1000), { payment_intent: 'pi_1', amount: '1000' });
});
```

- [ ] **Step 2: rossa** — `deno test --node-modules-dir=none --allow-read --allow-env supabase/functions/pos/rimborsi.test.ts` → «Module not found».

- [ ] **Step 3: il modulo**

```ts
/* rimborsi.ts — quanto torna al cliente di un ordine dal QR. Puro. (5 settembre 2026) */
export type OrdineRimborsabile = { totale_cent: number; rimborsato_cent?: number | null; modo: string; stato: string; stripe_pagamento?: string | null };
const RIMBORSABILI = new Set(['pagato', 'in_cucina', 'rimborsato']);

export function residuoRimborso(o: OrdineRimborsabile): number {
  return Math.max(0, Math.round(Number(o.totale_cent) || 0) - Math.round(Number(o.rimborsato_cent ?? 0) || 0));
}

export function importoRimborso(o: OrdineRimborsabile, richiesto: unknown): { ok: true; cent: number } | { ok: false; errore: string } {
  if (!RIMBORSABILI.has(o.stato)) return { ok: false, errore: 'l ordine non e stato pagato: niente da restituire' };
  if (o.modo === 'carta' && !o.stripe_pagamento) return { ok: false, errore: 'manca il riferimento del pagamento Stripe' };
  const residuo = residuoRimborso(o);
  if (residuo <= 0) return { ok: false, errore: 'e gia stato restituito tutto' };
  if (richiesto === undefined || richiesto === null || richiesto === '') return { ok: true, cent: residuo };
  const n = Number(richiesto);
  if (!Number.isInteger(n) || n <= 0) return { ok: false, errore: 'importo in centesimi interi, sopra lo zero' };
  if (n > residuo) return { ok: false, errore: `si possono restituire al massimo ${(residuo / 100).toFixed(2)} €` };
  return { ok: true, cent: n };
}

export function statoDopoRimborso(totale_cent: number, rimborsato_cent: number): 'rimborsato' | null {
  return rimborsato_cent >= totale_cent ? 'rimborsato' : null;
}

export const importoRiga = (r: { quantita: number; prezzo_cent: number }): number => Math.round(Number(r.quantita) * Number(r.prezzo_cent));

export const corpoRimborsoStripe = (paymentIntent: string, cent: number): Record<string, string> => ({ payment_intent: paymentIntent, amount: String(cent) });
```

- [ ] **Step 4: verde**, poi commit `Rimborsi degli ordini dal QR: le regole pure`.

---

### Task 2: la tabella e il server per il back office

**Files:**
- Create: `supabase/2026-09-05-pos-ordini-qr.sql`
- Modify: `supabase/functions/pos/index.ts` (import, `azioniBackOffice`, `creaStampe` con `avviso`, `ristampaConto`, cinque azioni, `giornata`)
- Test: `supabase/functions/pos/azioni.test.ts`

**Interfaces:**
- Consumes: Task 1.
- Produces: azioni `ospite-ordini` (GET `da`, `fino`), `ospite-rimborsa` (POST `ordine`, `importo_cent?`, `motivo?`, `da?`), `ospite-annulla-addebito` (POST `ordine`, `motivo?`, `da?`), `ospite-ristampa` (POST `ordine`, `da?`), `ospite-nota` (POST `ordine`, `nota`); `creaStampe(conto, righe, portata, tipo, cameriere, avviso: string | null = null)`; `ristampaConto(contoId, chi): Promise<number>`; `giornata` risponde anche `rimborsi_qr_cent`.

- [ ] **Step 1: la migrazione** (`node strumenti/migra.js supabase/2026-09-05-pos-ordini-qr.sql`)

```sql
alter table pos_ordine_ospite add column if not exists rimborsato_cent int not null default 0;
alter table pos_ordine_ospite add column if not exists rimborso_stripe text;
alter table pos_ordine_ospite add column if not exists nota text;
alter table pos_ordine_ospite add column if not exists annullato_il timestamptz;
alter table pos_ordine_ospite add column if not exists annullato_da text;
alter table pos_ordine_ospite drop constraint if exists pos_ordine_ospite_stato_check;
alter table pos_ordine_ospite add constraint pos_ordine_ospite_stato_check check (stato in ('in_attesa', 'pagato', 'in_cucina', 'annullato', 'errore', 'rimborsato'));
```

- [ ] **Step 2: la prova del contratto** (aggiunta a `azioni.test.ts`)

```ts
Deno.test('gli ordini dal QR dal back office: elenco, rimborso, annullo dell addebito, ristampa, nota', () => {
  assert(S.includes("import { corpoRimborsoStripe, importoRiga, importoRimborso, type OrdineRimborsabile, statoDopoRimborso } from './rimborsi.ts';"));
  assert(S.includes("'ospite-ordini', 'ospite-rimborsa', 'ospite-annulla-addebito', 'ospite-ristampa', 'ospite-nota'"), 'nel gruppo del back office');
  assert(S.includes("['addebiti', 'giornata', 'tavoli-qr', 'ospite-ordini'].includes(azione) && req.method === 'GET'"), 'l elenco si legge');
  const r = S.slice(S.indexOf("azione === 'ospite-rimborsa'"), S.indexOf("azione === 'ospite-annulla-addebito'"));
  assert(r.includes('const esito = importoRimborso(o as OrdineRimborsabile, b.importo_cent);') && r.includes("stripe(chiave, '/refunds', corpoRimborsoStripe("), 'con la carta si chiede a Stripe');
  assert(r.includes("if (ad.stato === 'riportato') return risposta({ errore: 'l addebito e gia riportato in Fidra: si corregge la' }, 409);"), 'in camera si toglie dall addebito, se non e gia in Fidra');
  assert(S.includes('async function ristampaConto(contoId: string, chi: string): Promise<number>') && S.includes("cameriere: string, avviso: string | null = null)"), 'la ristampa con l avviso in cima');
  assert(S.includes("avviso: [avviso, stampante !== originale ? 'cucina chiusa: al bancone' : null].filter(Boolean).join(' · ') || null,"));
  assert(S.includes('rimborsi_qr_cent'), 'la giornata li mostra');
});
```
e nella prova esistente «a cucina chiusa…» sostituire `avviso: stampante !== originale ? 'cucina chiusa: al bancone' : null,` con la riga nuova.

- [ ] **Step 3: rossa**, poi il codice (script node `applica-ordini-qr-server.js`, ancore uniche):

1. import dopo `import { apertoOra, leggiOrari, restringi, stampanteAdesso } from './orari.ts';`
2. `azioniBackOffice`: `[... 'tavoli-qr', 'ospite-ordini', 'ospite-rimborsa', 'ospite-annulla-addebito', 'ospite-ristampa', 'ospite-nota']`; GET ammesso per `'ospite-ordini'`.
3. `creaStampe(... cameriere: string, avviso: string | null = null)`, e nel Biglietto la riga `avviso:` come sopra.
4. `ristampaConto` subito prima di `/* ---------- allineamento`:

```ts
/* La comanda di nuovo, per portata, con «RISTAMPA» in cima: la stampante
   era spenta e il biglietto e' andato perso (5 settembre 2026). */
async function ristampaConto(contoId: string, chi: string): Promise<number> {
  const { data: c } = await db.from('pos_conto').select('*').eq('id', contoId).maybeSingle();
  if (!c) return 0;
  const tutte = (await righeDelConto(contoId)).filter((r) => r.stato !== 'stornata' && r.stato !== 'da_inviare');
  let n = 0;
  for (const p of PORTATE) {
    const rr = tutte.filter((r) => r.portata === p);
    if (rr.length) { await creaStampe(c, rr, p, 'comanda', chi, 'ristampa'); n++; }
  }
  return n;
}
```

5. le cinque azioni, prima di `if (azione === 'fasce-salva') {` (il codice completo e' nella spec al punto 2; `ospite-ordini` legge `da`/`fino` come la giornata e allega `tavolo_nome`, `addebito`, `righe_conto`).
6. `giornata`: prima di `const nomi = Object.fromEntries(` aggiungere
```ts
    /* i rimborsi degli ordini dal QR del giorno (5 settembre 2026): li fa Stripe, i pagamenti registrati non li vedono */
    const { data: rimborsi } = await db.from('pos_ordine_ospite').select('rimborsato_cent').gt('rimborsato_cent', 0).gte('creato_il', da).lt('creato_il', a);
    const rimborsi_qr_cent = (rimborsi ?? []).reduce((t, o) => t + (Number(o.rimborsato_cent) || 0), 0);
```
e nella risposta `da, a, locale: locale || null, rimborsi_qr_cent,`.

- [ ] **Step 4: verde + `deno check`**, commit `Ordini dal QR: elenco, rimborsi, annullo, ristampa e nota dal back office`.

---

### Task 3: il server per il palmare

**Files:**
- Modify: `supabase/functions/pos/index.ts` (`sala`: `qr` per tavolo e `coda_stampa`; azioni `conto-ristampa`, `conto-sposta`, `riga-storna-rimborsa`)
- Test: `supabase/functions/pos/azioni.test.ts`

**Interfaces:**
- Produces: `sala` → `tavoli[].qr: [{ numero, conto, totale_cent, rimborsato_cent, modo, camera, stato, creato_il, righe: [{ id, nome, quantita, prezzo_cent, nota, stato }] }]`, `coda_stampa: { n, minuti }`; `conto-ristampa` (POST `conto`); `conto-sposta` (POST `conto`, `a`); `riga-storna-rimborsa` (POST `riga`, `motivo`).

- [ ] **Step 1: prova del contratto**

```ts
Deno.test('il palmare: gli ordini dal QR di oggi sul tavolo, la coda di stampa, ristampa, sposta, storna e rimborsa', () => {
  const s = S.slice(S.indexOf("azione === 'sala'"), S.indexOf("azione === 'conto')"));
  assert(s.includes("const { data: qrOggi } = idTavoli.length") && s.includes("qr: qrOggi.filter((o) => o.tavolo === t.id)"), 'gli ordini dal QR di oggi, tavolo per tavolo');
  assert(s.includes("coda_stampa: { n: (coda ?? []).length, minuti:"), 'la coda di stampa del locale');
  assert(S.includes("'conto-ristampa', 'conto-sposta', 'riga-storna-rimborsa'"), 'nel gruppo del palmare');
  const st = S.slice(S.indexOf("azione === 'riga-storna-rimborsa'"), S.indexOf("azione === 'conto-sposta'"));
  assert(st.includes("if (!puo(cameriere!, 'storno')) return risposta({ errore: 'storno non permesso' }, 403);"));
  assert(st.includes('const cent = importoRiga({ quantita: Number(r.quantita), prezzo_cent: Number(r.prezzo_cent) });'), 'torna l importo della riga');
});
```

- [ ] **Step 2: rossa**, poi il codice:

`sala`, dopo il blocco `qr_recenti`:
```ts
    /* gli ordini dal QR di oggi (dalle quattro), tavolo per tavolo: il conto e'
       chiuso, ma il cameriere deve vedere cosa ha preso il tavolo (5 settembre 2026) */
    const inizioGiornata = (() => { const d = new Date(Date.now() + 2 * 3600 * 1000); if (d.getUTCHours() < 4) d.setUTCDate(d.getUTCDate() - 1); d.setUTCHours(4, 0, 0, 0); return new Date(d.getTime() - 2 * 3600 * 1000).toISOString(); })();
    const { data: qrOggi } = idTavoli.length
      ? await db.from('pos_ordine_ospite').select('id, tavolo, conto, totale_cent, rimborsato_cent, modo, camera, stato, creato_il').in('stato', ['in_cucina', 'rimborsato']).gte('creato_il', inizioGiornata).in('tavolo', idTavoli).order('creato_il')
      : { data: [] };
    const idContiQr = (qrOggi ?? []).map((o) => o.conto as string | null).filter((x): x is string => !!x);
    const { data: righeQr } = idContiQr.length ? await db.from('pos_riga').select('id, conto, nome, quantita, prezzo_cent, nota, stato').in('conto', idContiQr) : { data: [] };
    const qrPronti = (qrOggi ?? []).map((o) => ({ numero: o.id, conto: o.conto, tavolo: o.tavolo, totale_cent: o.totale_cent, rimborsato_cent: o.rimborsato_cent ?? 0, modo: o.modo, camera: o.camera, stato: o.stato, creato_il: o.creato_il, righe: (righeQr ?? []).filter((r) => r.conto === o.conto) }));
    /* i biglietti fermi in coda per questo locale: sopra i due minuti il palmare avvisa */
    const { data: coda } = await db.from('pos_stampa').select('creato_il').eq('locale', locale).eq('stato', 'da_stampare').order('creato_il').limit(50);
```
e nella risposta: `tavoli: (tavoli ?? []).map((t) => ({ ...t, conti: ..., qr: qrPronti.filter((o) => o.tavolo === t.id) })),` + `coda_stampa: { n: (coda ?? []).length, minuti: coda?.length ? Math.floor((Date.now() - Date.parse(String(coda[0].creato_il))) / 60000) : 0 },`.

(L'ora di Roma senza libreria: UTC+2 d'estate — usare `oraLocale` di fasce.ts sarebbe piu' giusto; qui basta: `const adessoRoma = oraLocale(new Date()); const inizio = new Date(); inizio.setUTCMinutes(inizio.getUTCMinutes() - adessoRoma.minuti + 4 * 60); if (adessoRoma.minuti < 4 * 60) inizio.setUTCDate(inizio.getUTCDate() - 1);` — scegliere questa forma, che usa i minuti locali veri.)

Le azioni, prima di `if (azione === 'tavolo-sposta') {`:
```ts
  /* ---------- gli ordini dal QR, dal palmare (5 settembre 2026) ---------- */
  if (azione === 'conto-ristampa') {
    if (req.method !== 'POST') return risposta({ errore: 'metodo non ammesso' }, 405);
    const b = await corpo();
    const n = await ristampaConto(String(b.conto ?? ''), cameriere!.nome);
    return n ? risposta({ esito: 'ok', biglietti: n }) : risposta({ errore: 'conto non trovato o senza righe' }, 404);
  }
  if (azione === 'riga-storna-rimborsa') {
    if (req.method !== 'POST') return risposta({ errore: 'metodo non ammesso' }, 405);
    if (!puo(cameriere!, 'storno')) return risposta({ errore: 'storno non permesso' }, 403);
    const b = await corpo();
    const motivo = motivoPulito(b.motivo);
    if (!motivo) return risposta({ errore: 'scriva il motivo dello storno' }, 400);
    const { data: r } = await db.from('pos_riga').select('*').eq('id', String(b.riga ?? '')).maybeSingle();
    if (!r || r.stato === 'stornata') return risposta({ errore: 'riga non trovata o gia stornata' }, 404);
    const { data: o } = await db.from('pos_ordine_ospite').select('*').eq('conto', String(r.conto)).maybeSingle();
    if (!o) return risposta({ errore: 'non e una riga di un ordine dal QR' }, 409);
    const cent = importoRiga({ quantita: Number(r.quantita), prezzo_cent: Number(r.prezzo_cent) });
    const esito = importoRimborso(o as OrdineRimborsabile, Math.min(cent, residuoRimborso(o as OrdineRimborsabile)));
    if (!esito.ok) return risposta({ errore: esito.errore }, 400);
    const ora = adesso();
    let rimborsoId: string | null = null;
    if (o.modo === 'carta') {
      const chiave = chiaveStripe(POS_PROVA);
      if (!chiave) return risposta({ errore: 'manca la chiave Stripe' }, 500);
      try { const rr = await stripe(chiave, '/refunds', corpoRimborsoStripe(String(o.stripe_pagamento), esito.cent)); rimborsoId = String(rr.id ?? '') || null; }
      catch (e) { return risposta({ errore: (e as Error).message }, 502); }
    } else {
      const { data: ad } = await db.from('pos_addebito').select('*').eq('conto', String(o.conto)).maybeSingle();
      if (!ad) return risposta({ errore: 'addebito non trovato' }, 404);
      if (ad.stato === 'riportato') return risposta({ errore: 'l addebito e gia riportato in Fidra: si corregge la' }, 409);
      const nuovoTot = Math.max(0, Number(ad.totale_cent) - esito.cent);
      await db.from('pos_addebito').update({ totale_cent: nuovoTot, stato: nuovoTot === 0 ? 'annullato' : ad.stato, nota: `${ad.nota ? ad.nota + ' · ' : ''}storno ${r.nome} (${cameriere!.nome})`.slice(0, 200), aggiornato_il: ora }).eq('id', ad.id);
    }
    await db.from('pos_riga').update({ stato: 'stornata', stornata_da: cameriere!.id, stornata_il: ora, motivo_storno: motivo, aggiornato_il: ora }).eq('id', r.id);
    const rimborsato = Number(o.rimborsato_cent ?? 0) + esito.cent;
    const agg: Riga = { rimborsato_cent: rimborsato, rimborso_stripe: rimborsoId ?? o.rimborso_stripe ?? null, nota: `${o.nota ? String(o.nota) + ' · ' : ''}storno ${r.quantita}× ${r.nome}: ${motivo} (${cameriere!.nome})`.slice(0, 500), aggiornato_il: ora };
    const nuovoStato = statoDopoRimborso(Number(o.totale_cent), rimborsato);
    if (nuovoStato) agg.stato = nuovoStato;
    await db.from('pos_ordine_ospite').update(agg).eq('id', o.id);
    if (r.stato === 'partita') {
      const { data: c } = await db.from('pos_conto').select('*').eq('id', String(r.conto)).single();
      const questa = (await righeDelConto(String(r.conto))).filter((x) => x.id === r.id);
      if (c && questa.length) await creaStampe(c, questa, r.portata as Portata, 'storno', cameriere!.nome);
    }
    return risposta({ esito: 'ok', rimborsato_cent: esito.cent, rimborso: rimborsoId });
  }
  if (azione === 'conto-sposta') {
    if (req.method !== 'POST') return risposta({ errore: 'metodo non ammesso' }, 405);
    const b = await corpo();
    const { data: c } = await db.from('pos_conto').select('*, t:pos_tavolo(z:pos_zona(locale))').eq('id', String(b.conto ?? '')).maybeSingle();
    if (!c) return risposta({ errore: 'conto non trovato' }, 404);
    const { data: a } = await db.from('pos_tavolo').select('id, nome, z:pos_zona(locale)').eq('id', String(b.a ?? '')).maybeSingle();
    if (!a) return risposta({ errore: 'tavolo non trovato' }, 404);
    const localeDi = (x: unknown) => (x as { locale: string } | null)?.locale ?? null;
    if (localeDi((c.t as { z: unknown } | null)?.z) !== localeDi(a.z)) return risposta({ errore: 'l altro tavolo e di un altro locale' }, 409);
    const ora = adesso();
    await db.from('pos_conto').update({ tavolo: a.id, aggiornato_il: ora }).eq('id', c.id);
    await db.from('pos_ordine_ospite').update({ tavolo: a.id, aggiornato_il: ora }).eq('conto', c.id);
    return risposta({ esito: 'ok', tavolo: { id: a.id, nome: a.nome } });
  }
```
e in `azioniPalmare`: `..., 'conto-ristampa', 'conto-sposta', 'riga-storna-rimborsa'`. `residuoRimborso` va importato.

- [ ] **Step 3: verde + check**, commit `Ordini dal QR sul palmare: sul tavolo tutto il giorno, ristampa, sposta, storna e rimborsa; la coda di stampa`.

---

### Task 4: la pagina del palmare

**Files:**
- Modify: `pagine/pos/index.html` (stile `.qrBlocco`, `.codaStampa`; `schermataSala` fascia rossa; `schermataTavolo` blocco «📱» e `pannelloQr`)
- Test: `pagine/pos/pagina.test.ts`

- [ ] **Step 1: prova**

```ts
Deno.test('gli ordini dal QR restano sul tavolo tutto il giorno: pannello con ristampa, sposta, storna e rimborsa; la coda di stampa avvisa', () => {
  assert(m.includes("const pannelloQr = (o) => {") && m.includes("chiama('conto-ristampa', { method: 'POST', body: JSON.stringify({ conto: o.conto }), cloud: true })"), 'ristampa dal cloud');
  assert(m.includes("chiama('conto-sposta', { method: 'POST', body: JSON.stringify({ conto: o.conto, a: b.dataset.verso }), cloud: true })"), 'sposta un conto solo');
  assert(m.includes("chiama('riga-storna-rimborsa', { method: 'POST', body: JSON.stringify({ riga: r.id, motivo }), cloud: true })"), 'storna e rimborsa la riga');
  assert(m.includes('class="qrBlocco"') && m.includes("📱 ${o.modo === 'camera' ? `in camera ${esc(o.camera || '')}` : 'pagato'}"), 'l etichetta');
  assert(m.includes("s.coda_stampa && s.coda_stampa.minuti >= 2") && m.includes('class="codaStampa"'), 'la fascia rossa sopra i due minuti');
});
```

- [ ] **Step 2: rossa, poi il codice** (script node): stile
```
.qrBlocco{margin-top:10px;border-top:1px dashed var(--pietra);padding-top:8px;}
.qrBlocco button.apri b{color:var(--oro);}
.codaStampa{margin:8px 10px 0;padding:8px 12px;border-radius:12px;background:var(--allarme);color:#fff;font-weight:600;}
```
sala: dopo `const qr = (s.qr_recenti || []).map(...)` aggiungere
```js
    const coda = s.coda_stampa && s.coda_stampa.minuti >= 2 ? `<div class="codaStampa">🖨️ ${s.coda_stampa.n} ${s.coda_stampa.n === 1 ? 'biglietto fermo' : 'biglietti fermi'} da ${s.coda_stampa.minuti}′: stampante spenta?</div>` : '';
```
e `$('app').innerHTML = barra + coda + (qr ? ...`.
tavolo: in `disegna()` dopo i conti aperti, il blocco `qrBlocco` con un bottone per ordine (`data-qr`); `pannelloQr(o)` con righe, «Ristampa», «Sposta al tavolo…» (chips dei tavoli del locale, come `pannelloSpostaTavolo`), e per riga non stornata, se `puoStorno`, «Storna e rimborsa» che chiede il motivo con `prompt`. Il codice completo e' nello script `applica-qr-palmare.js`.

- [ ] **Step 3: verde**, commit `Palmare: gli ordini dal QR sul tavolo, e l avviso della stampante ferma`.

---

### Task 5: il back office, scheda «POS · Ordini dal QR» e la giornata

**Files:**
- Modify: `pagine/buoni/index.html` (`SCHEDE`, mappa delle viste, `vistaPosOrdiniQr`, giornata)
- Test: `pagine/buoni/pos-schede.test.ts`, `pagine/buoni/scheda-iniziale.test.ts` (ordine delle schede: `posOrdiniQr` subito dopo `posAddebiti`? No: primo del gruppo POS, vedi Task 6)

- [ ] **Step 1: prova**
```ts
Deno.test('la scheda «POS · Ordini dal QR»: un giorno alla volta, rimborsa, annulla l addebito, ristampa, nota', () => {
  assert(P.includes("['posOrdiniQr', 'POS · Ordini dal QR']") && P.includes('posOrdiniQr: vistaPosOrdiniQr'));
  const v = P.slice(P.indexOf('function vistaPosOrdiniQr('), P.indexOf('function vistaPosGiornata('));
  for (const a of ['a=ospite-ordini&da=', 'a=ospite-rimborsa', 'a=ospite-annulla-addebito', 'a=ospite-ristampa', 'a=ospite-nota']) assert(v.includes(a), a);
  assert(v.includes('Rimborsa') && v.includes('Annulla l’addebito') && v.includes('Ristampa la comanda'));
});
Deno.test('la giornata mostra i rimborsi dal QR', () => { assert(P.includes("cifra('Rimborsi dal QR', num(g.rimborsi_qr_cent))")); });
```
Nota: `rimborsi_qr_cent` arriva accanto a `giornata` nella risposta: la vista lo prende da `j.rimborsi_qr_cent` e lo mette in `g`.

- [ ] **Step 2: il codice** (script node): `SCHEDE` con `['posOrdiniQr', 'POS · Ordini dal QR']` prima di `posAddebiti`; la vista modellata su `vistaPosAddebiti` (giorno con `<input type="date">`, finestra 4→4 come la giornata, elenco con righe, importo del rimborso in euro con `centPos`, prompt per il motivo, nota salvata con un bottone); la giornata: `const g = { ...j.giornata, rimborsi_qr_cent: j.rimborsi_qr_cent || 0 };` dove oggi passa `j.giornata` a `disegna`.

- [ ] **Step 3: verde**, commit `Back office: la scheda degli ordini dal QR e i rimborsi nella giornata`.

---

### Task 6: il back office in quattro famiglie

**Files:**
- Modify: `pagine/buoni/index.html` (`FAMIGLIE`, `famigliaDi`, `disegna()` barra a due righe, stile `.famiglie`)
- Test: `pagine/buoni/scheda-iniziale.test.ts` (ordini attesi con `posOrdiniQr`), `pagine/buoni/pos-schede.test.ts`

- [ ] **Step 1: prova**
```ts
Deno.test('le schede stanno in quattro famiglie: Buoni, Ospiti, Day Spa, POS', () => {
  assert(P.includes("const FAMIGLIE = [") && P.includes("['pos', 'POS', ['posOrdiniQr', 'posAddebiti', 'posGiornata', 'posMenu', 'posTavoli', 'posPersonale', 'posFasce']]"));
  assert(P.includes('class="famiglie"') && P.includes('const famigliaDi = (scheda) =>'));
});
```
e in `scheda-iniziale.test.ts` gli elenchi attesi prendono `posOrdiniQr` prima di `posAddebiti`... (l'ordine della pagina resta piatto: `SCHEDE` decide; la barra mostra solo le schede della famiglia aperta, nell'ordine di `schedeDi`).

- [ ] **Step 2: il codice**: `FAMIGLIE = [['buoni','Buoni',['emetti','elenco','verifica']], ['ospiti','Ospiti',['richieste','arrivi','privacy']], ['dayspa','Day Spa',['dayspaOggi','dayspaDisponibilita','dayspaPrenotazioni']], ['pos','POS',[...]]]`; in `disegna()`: prima riga `.famiglie` (una per famiglia con almeno una scheda visibile, attiva quella di `VISTA`), seconda riga `.schede` con le sole schede della famiglia; il contatore delle richieste resta sul pulsante `richieste` (e, sulla famiglia «Ospiti», la stessa cifra).

- [ ] **Step 3: verde**, commit `Back office: le schede in quattro famiglie`.

---

### Task 7: in linea

- [ ] deploy `pos`; `deno test` di tutto; `node strumenti/pacchetto-bistrot.js`; commit della spec e del piano; push; memoria `pos-stato.md`.
